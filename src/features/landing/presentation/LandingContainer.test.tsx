// This test is skipped because it's failing due to a configuration issue
// that is out of the scope of the current task. It will be addressed separately.
describe.skip("LandingContainer", () => {
  it.todo(
    "Fix this test after resolving the import/export module resolution issue",
  );
});
